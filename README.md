This is a severe weather tracker designed to... track severe weather.
The goal is to allow people to report severe weather to better warn people
  The app uses the Leaflet map to allow the user to click the map to assign the report to a location 
  The info is stored in the localStorage of the browser. For full deployments, the data should be stored in a database to allow other users to access reports. 
  
